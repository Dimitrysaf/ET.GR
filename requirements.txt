# FEK Processor – Python dependencies
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# PDF / OCR
pymupdf>=1.24.0          # fitz
pytesseract>=0.3.10
Pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0  # cv2 (headless – no GUI deps)

# XML
lxml>=5.2.0

# AI / Ollama
ollama>=0.2.1            # pinned to object-API era

# System
psutil>=5.9.0

# Markitdown
markitdown[all]>=0.1.6
openai